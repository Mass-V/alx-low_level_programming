#include "main.h"

/**
 * mul - multiplies between two values of input
 * @a: number input
 * @b: number input
 * Return: value of mul
 */
int mul(int a, int b)
{
	int multy;

	multy = a * b;
	return (multy);
}
