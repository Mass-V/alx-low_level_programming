#include <stdio.h>

/**
 * main - prints single digits of base 10 beginning from 0
 *
 * Return: always zer0
 */
int main(void)
{
	int a;

	for (a = 0; a <= 9; a++)
	{
		printf("%d", a);
	}

	printf("\n");

	return (0);
}
